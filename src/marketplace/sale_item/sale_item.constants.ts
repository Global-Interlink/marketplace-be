export enum SaleItemType {
    NFT = 'nft'
}

export enum SaleItemState {
    ON_SALE = 'on_sale',
    SALE_DONE = 'sale_done',
    CANCELLED = 'cancelled',
    NEED_TO_RE_COMFIRM = 'need_to_re_confirm',
    VERIFING = 'verifing'
}

export enum SaleItemBuyType {
    BUY_NOW = 'buy_now',
    AUCTION = 'auction'
}